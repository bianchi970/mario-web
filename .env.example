# URL del mario-hub (server-side only)
HUB_URL=http://localhost:4001

# Bearer token per autenticarsi all'hub (server-side only, mai al browser)
HUB_TOKEN=dev-token

# Project ID default
DEFAULT_PROJECT_ID=default

# URL display pubblico (opzionale)
NEXT_PUBLIC_HUB_DISPLAY_URL=http://localhost:4001
